version: 0.2
phases:
  install:
    runtime-versions:
      php: 7.3
    commands:
      - npm install
      - npm install -g serverless
  #  pre_build:
  #    commands:
  #      - command
  build:
    commands:
      - . ./build.sh
#  post_build:
#    commands:
#      - . ./test.sh
